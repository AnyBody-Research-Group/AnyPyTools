package:
  name: anypytools     # lower case name of package, may contain '-' but no spaces
  version: 0.5        # version of package. Should use the PEP-386 verlib conventions.


build:            # (optional)
  number: 0                        # (optional, defaults to 0)

# the build and runtime requirements:
requirements:     # (optional)
  build:
    - python
    - setuptools
    

  run:

#test:
#  # Python imports
#  imports:
#    - anypytools

about:            # (optional)
  home: https://github.com/AnyBody-Research-Group/AnyPyTools
  license: MIT